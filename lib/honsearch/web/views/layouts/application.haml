!!!
%html
  %head
    %meta(charset="UTF-8")
    %title= "#{page_title}"
    %meta(name="viewport" content="width=device-width" initial-scale="1.0")
    - if url_for(only_path: true) == "/"
      %meta(name="description" content="書誌情報を検索できるサイト。著者や出版社での絞り込み機能付き。全文検索エンジンとしてGroongaを使用。")
      %meta(name="keywords" content="書誌情報,検索,全文検索")
    - else
      %meta(name="robots" content="noindex")
    %link(href="#{@sub_url}vendor/bootstrap/css/bootstrap.min.css"
          rel="stylesheet" media="screen")
    %link(href="#{@sub_url}css/honsearch.css" rel="stylesheet" media="screen")
    %link(rel="shortcut icon" type="image/vnd.microsoft.icon" href="favicon.ico")
  %body
    = render :_header, layout: false

    .container
      = yield

    #footer.container.text-right
      = render :_footer, layout: false
